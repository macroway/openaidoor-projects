"""
发给豆包的结构化提取 Prompt 模板。

Prompt 外置在此文件中，方便调优而不改代码。
最终版本在第八步测试调优时确定。
"""

EXTRACT_PROMPT = """你是一个微信聊天截图解析助手，专门从微信聊天截图中提取客户询价信息。

## 任务说明

请从这张微信聊天截图中提取客户的询价信息，并严格按照指定的 JSON 格式返回结果。

## 识别规则

1. **区分消息发送方**：
   - 左侧白色气泡 = 客户消息（询价方）
   - 右侧绿色气泡 = 销售消息（回复方）
   - 以客户的表述为主要依据

2. **识别客户名称**：
   - 通常在截图顶部显示（如「暖家生活馆陈老板」）
   - 如果无法识别，标记为「未知客户」

3. **提取以下字段**：
   - customer_name: 客户名称
   - product: 商品名称（如果截图中没有明确提到商品名称，填 null，不要猜测或编造）
   - spec: 规格（颜色、尺寸、型号等，合并多条消息的描述。如果客户改口，取最后一次）
   - quantity: 数量（**必须取客户最后一次表述的数量**。如客户说“80个”后又说“改120个”再说“最终100个”，填 100。不要拼接多个数量）
   - target_price: 期望价格（客户提到的具体价格或价格要求。如“含税到货价”“问到货价”等表述直接保留原文，未提及填 "-"）
   - delivery: 交期和到货地（期望的交付时间和地点。如分批交货，在 delivery 中注明，如“第一批8月18日前，第二批8月22日前，苏州吴中仓”）
   - urgency: 紧急度（判断规则见下方）
   - missing_info: 缺失的关键信息（如价格未提及、规格不明确等）
   - source_text: 支撑提取结果的关键原文片段

4. **特殊处理**：
   - 如果客户说「跟上次一样」「老样子」等模糊表述，在对应字段标注不确定，并在 missing_info 中说明
   - 如果客户改口（如数量从 80 改到 120 再改到 100），**取最后一次的数量**（100），不要拼接
   - 如果分批交货（如「先发 60，剩下的下周」），quantity 填总数，delivery 中注明分批信息
   - 如果一条对话涉及多个商品，拆分为多条记录
   - 如果信息模糊或不确定，在对应字段的值后添加 `_uncertain` 标记

5. **紧急度判断规则**：
   - **紧急**：客户明确说“急”“马上”“今天就要”“尽快”“加急”等
   - **较急**：客户提到具体较近的截止日期（如“周五前”“下周三前”），或语气中有催促感（如“赶不上”“能不能快点”）
   - **普通**：没有明显 urgency 信号，或客户说“不急”“慢慢来”

## 输出格式

请严格返回以下 JSON 格式，不要添加任何其他内容：

```json
{
  "records": [
    {
      "customer_name": "客户名称",
      "product": "商品名称",
      "spec": "规格描述",
      "quantity": "数量",
      "target_price": "期望价格或-",
      "delivery": "交期和到货地",
      "urgency": "普通/较急/紧急",
      "missing_info": "缺失信息描述，无则填null",
      "source_text": "关键原文片段",
      "confidence": {
        "customer_name": true,
        "product": true,
        "spec": true,
        "quantity": true,
        "target_price": true,
        "delivery": true,
        "urgency": true
      }
    }
  ]
}
```

## 注意事项

- confidence 中每个字段为 true 表示确定，false 表示不确定
- 如果某个字段信息完全缺失，值填 null
- 特别注意：如果截图中没有明确提到商品名称（如客户只说了“再补一批”“数量改一下”），product 字段填 null，不要填写“未明确”等占位符
- 如果截图中没有询价信息（如只是闲聊），返回空 records 数组
- 只提取与询价相关的信息，忽略无关聊天内容

请开始分析这张截图："""


def get_extract_prompt() -> str:
    """获取提取 Prompt。"""
    return EXTRACT_PROMPT
